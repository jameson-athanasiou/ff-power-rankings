import React from 'react';
import { Table, TableBody, TableCell, TableRow, Paper } from '@material-ui/core';

const StandingsDetail = ({ data }) => (
    <Paper>
        <Table>
            <TableBody>

            </TableBody>
        </Table>
    </Paper>
);

export default StandingsDetail;
